spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/audio_service_db
    username: user
    password: qUu-MAX-7eU-PSW
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect